{
  "name": "tiny-spellbook",
  "version": "0.2.0",
  "private": true,
  "description": "Arc/Chrome extension for starting ChatGPT chats with reusable markdown skills.",
  "scripts": {
    "build:index": "node scripts/generate-skill-index.js",
    "build": "npm run build:index"
  }
}
